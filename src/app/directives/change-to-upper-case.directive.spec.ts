import { ChangeToUpperCaseDirective } from './change-to-upper-case.directive';

describe('ChangeToUpperCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeToUpperCaseDirective();
    expect(directive).toBeTruthy();
  });
});
